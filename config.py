blog_url = 'xxx'
blog_id = 'xxx'
username = 'xxx'
password = 'xxx'
gen_network_file = 0  # 是否生成图片替换后文件,默认关闭
publish = False  # 上传后是否发布，默认未发布，设置True为发布
img_tag = False  # 图片上传后转为<img>标签，设置width，居中
img_width = "100%"  # 图片宽度，默认100%，结合img_tag使用
